spring:
  application:
    name: workflow-demo
  
  sleuth:
    workflow-demo:
      probability: 1.0
    log:
      json:
        enabled: true
    
server:
  port: 10000

logging:
  level:
    org.springframework.web.servlet.DispatcherServlet: INFO